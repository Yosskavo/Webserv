#ifndef WEBSERV_H
# define WEBSERV_H

# include <iostream>
# include <fstream>
# include <exception>

typedef struct s_config {

} t_config;

t_config	ft_parce_config(const char *path_to_config);

#endif
